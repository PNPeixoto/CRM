# Sessão 2026-07-31 — Fundação de apresentação por segmento

## Entrega

- Perfil persistido do tenant e quatro presets versionados.
- APIs de apresentação/onboarding e contratos públicos imutáveis.
- Funil inicial por segmento com criação atômica e idempotente.
- Resolver único de navegação, onboarding e atualização sem novo login.
- Separação de papéis PostgreSQL para migrations e runtime.
- Lock pessimista na rotação de refresh token.
- Persistência da ingestão movida para bean transacional separado.
- Validação em aplicação e constraints compostas para referências multi-tenant.
- Testes backend e frontend cobrindo os novos contratos e corridas.

## Alterações locais preservadas

Antes desta sessão já estavam modificados `application-dev.yml` e V1, e já
existia uma V6 não versionada para `event_publication`. Essas alterações não
foram revertidas nem reescritas. A migration de perfil começou em V7 para não
colidir com esse estado real.

## Validação executada

- JDK 25.0.4.
- Docker Desktop 29.6.2 iniciado e PostgreSQL 17 saudável em
  `127.0.0.1:5432`; Memurai em `6379` respondeu `PONG`.
- Backend `dev` iniciou, aplicou migrations e respondeu `UP` no health check;
  a instância temporária foi encerrada depois.
- Suíte backend completa: 59 de 59 testes passaram com PostgreSQL e Redis reais
  via Testcontainers, incluindo RLS sob `crm_runtime_test` restrito.
- Frontend: 6 testes, build e lint: sucesso; lint com três avisos, zero erros.

## Correções reveladas pela integração

- Testcontainers separado entre credenciais de migration e runtime.
- Tratamento de retorno SQL nulo nas buscas de tenant e refresh token.
- JSON inválido/desconhecido agora resulta em 400, não 500.
- Fixtures da fila adaptadas para respeitar RLS.

## Pendência operacional

Escolher entre manter o Memurai local ou liberar a porta 6379 para o Redis do
Compose. O backend funciona com o Memurai; apenas o contêiner `crm-redis` fica
em estado `Created` enquanto a porta estiver ocupada.
